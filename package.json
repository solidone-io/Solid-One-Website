{
  "name": "workspace",
  "version": "0.0.0",
  "packageManager": "pnpm@10.32.1",
  "license": "MIT",
  "scripts": {
    "dev": "pnpm --filter @workspace/solid-one-web run dev",
    "build": "pnpm --filter @workspace/solid-one-web run build",
    "typecheck": "pnpm --filter @workspace/solid-one-web run typecheck"
  },
  "private": true,
  "devDependencies": {
    "prettier": "^3.8.3",
    "typescript": "~5.9.3"
  }
}
